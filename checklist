1. Anzianita: il file deve essre piu vecchio di un tot (24 ore per adesso)
2. Stato di Scrittura: il file non deve essere in fase di scrittura (non in open_file_dict)
3. Idealmente il file dovrebbe essere gia stato inviato all'api e all'ftp (non in fft_dict e in api_dict)

